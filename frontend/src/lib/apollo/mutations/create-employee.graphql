mutation UDATE_EMPLOYEE($input: UpdateEmployeeInput!) {
  updateEmployee(input: $input) {
    id
    firstName
    lastName
    salary
    jobTitle
    hireDate
    company {
      name
      id
      headquartersLocation
    }
  }
}